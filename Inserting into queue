#include <iostream>
#include <queue>

using namespace std;

int main() {
    queue<int> q;

    // Enqueueing elements into the queue
    q.push(5);
    q.push(10);
    q.push(15);

    // Displaying elements of the queue
    cout << "Queue elements:";
    while (!q.empty()) {
        cout << ' ' << q.front();
        q.pop();
    }
    cout << endl;

    return 0;
}
